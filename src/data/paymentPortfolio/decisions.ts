// payment-platform 포트폴리오 데이터 · 콘텐츠/지오메트리. 렌더 로직과 분리(유지보수·편집 격리).
// 정본 페이지: src/pages/payment-platform-portfolio/index.astro

export const DECISIONS = [
    {t:"재고 차감 후 저장 실패 시 자동 복구 배제",tag:"초과 판매 0",
     ctx:"· 재고 차감 성공 직후 DB 확정 저장이 실패한 극단적 장애 상황을 다루는 정책 (일반 PG 거절 등은 자동 복구 대상)",
     why:"· 자동 복구를 시도하다 서버가 다운되면 차감 기록은 남고 복구 내역은 유실\n· 원인 추적이 불가능한 영구적 불일치 위험",
     alt:"· 실패 시 무조건 재고 롤백 시도 (롤백 중 장애 시 유실 위험)",
     tradeoff:"· 확정 저장(DB 커밋)이 예외로 실패하면 catch 지점에서 미복구 카운터 증가 + error 로그, 재고는 보상 없이 차감 상태 유지\n· Prometheus에 상시 노출돼 값이 오르면 운영자가 원인 확인 후 수동 회수\n· 재고 일시 과소 표기는 감수, 초과 판매는 원천 차단"},
    {t:"결제 실패 시 '재고 복원 선행, 상태 기록 후행' 원칙",tag:"재고 유실 0",
     ctx:"· PG 승인 거절 등으로 결제를 최종 실패(FAILED) 처리하고 재고를 복원하는 정상 복구 경로의 순서 문제",
     why:"· 상태를 먼저 '실패'로 갱신하면 직후 장애 시 재고 복원 로직 미실행\n· 결제는 취소, 재고는 차감된 채 남는 유실 발생",
     alt:"· 결제 실패 상태 기록 후 재고 복원 (순서 반대)",
     tradeoff:"· 재고 복원을 먼저 수행 후 결제 상태 갱신 · 복원은 멱등 키(compensation:done:{orderId})를 SETNX로 남기고 INCRBY하는 단일 Lua\n· 상태 갱신 직전 크래시로 실패 처리가 재시도돼도 그 키가 이미 있으면 복원을 생략하여 정확히 한 번만 반영"},
    {t:"격리 건 복구 시 차감 키 기반 선택적 재고 복원",tag:"유령 재고 0",
     ctx:"· 격리된 결제를 일괄 실패 처리할 때 개별 건의 실제 재고 차감 여부 판별 문제",
     why:"· 격리 사유를 무시하고 맹목적으로 재고를 +1 복원하면 차감된 적 없는 건까지 복원\n· 실제 보유량보다 재고가 늘어나는 오류 발생",
     alt:"· 격리된 모든 결제건에 일괄 재고 복원 명령 수행",
     tradeoff:"· 재고 차감 시 Redis 재고 캐시에 남는 키(decrement:done) 존재 여부를 확인해 조건부 복원\n· 유령 재고 생성 원천 차단"},
    {t:"DLQ 메시지는 원본 토픽 재주입 방식으로 복구",tag:"검증된 경로 재사용",
     ctx:"· 최종 실패로 DLQ(Dead Letter Queue)에 적재된 메시지를 수동 재처리하는 아키텍처 구성 문제",
     why:"· DLQ 전용 컨슈머를 분리하면 기존 파이프라인의 멱등성·정확히 1회(Exactly-Once) 로직을 중복 구현·재검증 필요\n· 그만큼 파편화 리스크 발생",
     alt:"· DLQ 전용 별도 백그라운드 소비 워커 구축",
     tradeoff:"· 메시지를 원본 토픽(events.confirmed)으로 재발행해 검증된 메인 파이프라인 재사용\n· 보존 기한 지난 지연 메시지는 타임스탬프 기반 방어"},
    {t:"PG 재시도 횟수 상태의 DB 영속화",tag:"격리 도달 보장",
     ctx:"· 지수 백오프 기반 PG 재시도에서 현재 시도 횟수(Attempt)를 어디서 관리할지 결정",
     why:"· 시도 횟수를 메시지에만 실으면 재주입·중복 배달 때 카운트가 메시지마다 흩어져 한 주문 기준으로 누적되지 않음\n· 주문 단위 한도(4회) 판정이 어긋날 위험",
     alt:"· 시도 횟수를 Kafka 메시지 헤더로만 전달 (메시지 단위 상태)",
     tradeoff:"· 시도 횟수를 DB(pg_inbox.attempt)에 주문 단위로 중앙 관리\n· 재발행·재주입돼도 그 주문의 누적 횟수가 유지돼 한도(4회) 도달 보장"},
    {t:"Kafka EOS + 멱등 소비로 DB·발행 정합성 확보",tag:"정확히 1회",
     ctx:"· 결제 상태 갱신(DB)과 재고 확정 이벤트(Kafka) 발행 사이의 정합성 보장 방안\n· MySQL·Kafka는 한 트랜잭션으로 묶을 수 없는 이종 시스템 (물리적 2PC 미채택)",
     why:"· 두 커밋을 보정 없이 분리하면 그 사이 크래시에 DB·다운스트림 영구 불일치\n· 그렇다고 분산 2PC로 묶으면 운영·성능 비용 과다",
     alt:"· 분산 트랜잭션(2PC)으로 DB·Kafka를 물리적 원자 단위로 결합",
     tradeoff:"· DB 커밋 후 Kafka 커밋 전 크래시 시 재전송, 멱등 마킹(payment_event_dedupe · INSERT IGNORE)이 중복 처리 흡수\n· 성능 트레이드오프는 감수, 금융·재고 데이터 정합성 최우선"},
    {t:"MSA 환경 내 서비스 간 공통 모듈 의존성 완벽 분리",tag:"독립 배포",
     ctx:"· 4개 독립 마이크로서비스 간 유사한 DTO·도메인 객체의 공유 방식 결정",
     why:"· 공통 모듈(Core Library) 공유는 개발 초기엔 편리\n· 단일 의존성 병목으로 한 서비스 변경이 전체 재배포를 강제하는 결합 발생",
     alt:"· 공통 DTO·도메인을 묶은 Core Shared Module 운영",
     tradeoff:"· 코드 중복을 감수하고 독립성 선택\n· 통신은 오직 HTTP·Kafka 인터페이스 스펙으로만 결합해 개별 서비스 자율 배포 보장"},
    {t:"멱등 기록 보존을 Kafka 보관 기간보다 길게 (8일 > 7일)",tag:"재전송 안전",
     ctx:"· 재전송된 메시지를 걸러내는 멱등 기록의 저장소와 보존 기간 설정",
     why:"· 보존 기간이 Kafka 메시지 보관 기간보다 짧으면 아직 브로커에 남은 메시지가 재전송될 때 기록이 이미 만료돼 신규 메시지로 오인\n· 같은 메시지를 이중 처리할 위험",
     alt:"· 보존 기간을 Kafka retention과 같거나 짧게 설정",
     tradeoff:"· 멱등 기록을 payment·product 각 서비스 MySQL에 행으로 남기고, 스케줄러가 보존 기간 지난 행을 배치 삭제\n· 그 보존 기간을 Kafka retention(7일)보다 긴 8일로 고정(불변식 보존 > retention) · 브로커가 메시지를 버릴 때까지 기록이 살아 있어 재전송을 안전하게 흡수"}];
